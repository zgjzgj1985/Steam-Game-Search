---
description: 项目开发规范和数据保护规则
alwaysApply: true
version: v1.2.0
lastUpdated: 2026-04-29
---

# 开发规范

## 语言要求

本项目**所有输出**（包括 AI 对话框中显示的全部内容）必须使用**简体中文**。

### 具体要求

1. **AI 对话响应**：AI 发送给用户的每一条消息必须使用简体中文，包括解释、说明、建议、总结等
2. **代码注释与文档**：所有代码注释必须使用简体中文
3. **用户交互输出**：命令行输出、错误信息必须使用简体中文
4. **变量命名**：使用英文命名（符合项目惯例）
5. **文件内容**：所有新建的文件内容必须使用简体中文

### 禁止事项

- 禁止在 AI 对话输出中夹杂繁体中文、英文或其他语言（代码中的英文除外）
- 禁止在返回给用户的信息中使用非简体中文的通用问候语（如 "Hello", "Hi there" 等）
- 禁止在代码注释中写英文

### 违规处理

若 AI 响应中出现非简体中文内容，用户有权要求 AI 重新输出正确版本，AI 不得以任何理由拒绝。AI 应立即重新生成符合要求的简体中文响应。

### 正确的问候语示例

- 正确：「你好，有什么可以帮助你的吗？」
- 正确：「好的，我来帮你处理。」
- 正确：「让我先了解一下项目情况。」
- 错误：Hello / Hi there / 你好呀～ / 嗨

---

## 文档版本同步规则

### 核心原则

**每次代码变更后，必须同步更新相关文档版本号和变更日志。**

### 版本号定义

| 文件 | 版本号位置 | 格式 |
|------|-----------|------|
| `数据采集维护文档.md` | 文档顶部 `> **版本**: vX.Y.Z` | SemVer |
| `PROJECT.md` | 文档顶部 | SemVer |
| `README.md` | 文档顶部 | SemVer |

### 同步检查清单

每次代码变更涉及以下内容时，必须执行文档同步：

| 变更类型 | 必需同步 |
|----------|----------|
| 数据文件结构变更 | `数据采集维护文档.md` 版本号+变更日志 |
| API接口变更 | `PROJECT.md` + `数据采集维护文档.md` |
| 性能优化 | `数据采集维护文档.md` 版本号+变更日志 |
| 数据清洗/修复 | `数据采集维护文档.md` 版本号+变更日志 |
| 前端功能变更 | `PROJECT.md` |
| 任何数据文件修改 | `数据采集维护文档.md` 版本号+变更日志 |

### 同步操作步骤

1. **变更前**：读取相关文档，确认当前版本号（如 `v1.2.0`）
2. **代码变更**：执行代码修改
3. **版本递增**：根据变更性质决定版本号递增：
   - 主版本(`X.0.0`)：不兼容的API变更
   - 次版本(`X.Y.0`)：新增功能，向后兼容
   - 修订版本(`X.Y.Z`)：bug修复，向后兼容
4. **更新版本号**：在文档顶部更新版本号和更新日期
5. **记录变更日志**：在变更日志表格添加新行

### 版本号一致性检查

如果AI发现以下情况，必须主动报告：

- 代码版本与文档版本不一致
- 多个文档版本号不一致
- 变更日志缺失本次变更记录

### 示例

```
# 假设当前版本 v1.2.0，变更内容：优化了搜索算法

1. 读取 `数据采集维护文档.md`，确认版本 v1.2.0
2. 修改代码
3. 判定为次版本递增 → v1.3.0
4. 更新文档顶部：
   > **版本**: v1.3.0
   > **更新日期**: 2026-04-15
   > **版本变更说明**:
   > - v1.2.0: ...
   > - v1.3.0: 搜索算法优化，性能提升X%
5. 更新变更日志表格
```

---

# 数据文件保护规则

## 重要提醒

`public/data/` 文件夹包含大型数据文件（总计约1.6GB），**不在Git版本控制中**。

## 禁止执行的命令

- `git clean -fd` — 会删除未追踪的文件
- `git reset --hard` — 会重置工作区
- 删除 `public/data/` 文件夹

## 备份机制

- **自动备份**：每周日凌晨2:00执行（Windows任务计划程序 SteamDataBackup）
- **备份位置**：`D:\SteamDataBackup\`
- **保留期限**：最近4周
- **手动备份**：`python scripts/backup-data.py`

## 恢复数据

```bash
python scripts/backup-data.py status    # 查看备份列表
python scripts/backup-data.py restore backup_2026-04-15_092115
```

## 数据修改前必做

修改数据相关代码前，务必先执行手动备份：

```bash
python scripts/backup-data.py
```

---

## 理解优先原则

**解决问题之前，必须先理解项目。**

在动手修复任何问题前，应先：
- 阅读相关文档，了解项目背景和架构
- 查看现有代码结构，理解问题上下文
- 确认问题根因，避免治标不治本

> 盲目修改代码可能导致新的问题，理解和规划比快速行动更重要。

---

# 遵循原则

1. **语言一致性（最高优先级）**：所有 AI 对话输出必须使用简体中文，不得夹杂其他语言或方言
2. **理解优先**：解决问题前先理解项目背景和代码结构
3. **数据安全**：修改数据代码前先备份
3. **文档同步**：每次变更必须更新相关文档版本号和变更日志
4. **版本一致性**：确保代码版本与文档版本一致
5. **清晰表达**：用简洁中文描述技术概念

---

## 临时脚本管理

### 核心要求

项目开发过程中创建的临时脚本（如数据检查脚本、调试脚本、一次性处理脚本等）必须统一放到 `scripts/temp/` 文件夹下。

### 目录结构

```
scripts/
├── temp/                    # 临时脚本存放位置
│   ├── check_*.py          # 数据检查类脚本
│   ├── fix_*.py            # 数据修复类脚本
│   ├── analyze_*.py        # 分析类脚本
│   ├── test_*.py           # 测试类脚本
│   └── *.py                # 其他临时脚本
├── backup-data.py           # 正式脚本（保留在根目录）
└── precompute.py            # 正式脚本
```

### 判断标准

| 应放入 `scripts/temp/` | 应保留在 `scripts/` 根目录 |
|------------------------|---------------------------|
| 一次性使用的调试脚本 | 需要长期维护的核心脚本 |
| 临时数据修复脚本 | 集成到CI/CD的自动化脚本 |
| 检查验证脚本 | 基础设施脚本（备份、采集等） |

### 清理机制

**任务完成后必须立即清理本次任务创建的临时文件，不得遗留。**

- 每次任务完成后，检查本次新建的文件（测试文件、临时脚本、调试输出等），确认是否需要保留
- 临时验证用的测试文件（如 `tests/*.spec.ts`）和调试脚本使用后必须删除
- 除非用户明确要求保留，否则所有一次性使用的文件必须清理
- 如临时脚本演变为正式功能，应移动到 `scripts/` 根目录
- 定期检查 `scripts/temp/` 文件夹，删除已确认无用的临时脚本

---

## 任务状态持久化

### 核心目标

确保长时间任务不会因对话中断而丢失。每次对话开始时自动检查并恢复任务状态。

### 状态文件位置

- **当前任务**：`D:\Steam全域游戏搜索\.cursor\tasks\current-task.md`
- **状态说明**：`D:\Steam全域游戏搜索\.cursor\tasks\任务状态追踪.md`
- **历史记录**：`D:\Steam全域游戏搜索\.cursor\tasks\history\`

### 任务开始时（必须执行）

1. **读取当前任务状态**：检查 `current-task.md` 是否存在
2. **评估任务状态**：
   - 如果存在未完成任务，询问用户是要继续还是开始新任务
   - 如果没有未完成任务，开始新任务
3. **创建/更新任务状态文件**

### 任务进行中（必须执行）

**每完成一个关键步骤，立即更新 `current-task.md`：**

- 当前进度百分比
- 已完成的工作（带时间戳）
- 下一步计划
- 修改过的文件列表
- 任何遇到的问题

### 任务完成时（必须执行）

1. 更新任务状态为"已完成"
2. 添加任务总结
3. 将任务移到历史记录

### 禁止行为

- 在未更新状态文件的情况下结束长时间任务
- 覆盖用户未确认放弃的任务
- 将包含敏感信息的任务状态提交到 git

---

## 变更完成检查清单

每次代码变更完成后，**必须**执行以下检查：

```
[ ] 1. 代码变更是否属于需要同步文档的类别？
     （数据文件结构变更 / API接口变更 / 性能优化 / 数据清洗修复 / 前端功能变更 / 任何数据文件修改）
[ ] 2. 相关文档版本号是否已更新？
[ ] 3. 变更日志是否记录了本次变更？（做什么、什么时候、结果如何）
[ ] 4. 多个相关文档的版本号是否一致？
[ ] 5. 如有测试验证，测试结果是否已记录到文档中？
[ ] 6. 本次任务创建的临时文件是否已清理？（测试文件、调试脚本等）
```

**强制提示**：如果跳过以上任何一步，视为违反开发规范。

---

## 前置必做（强制执行）

每次执行代码变更任务前，**必须**按以下顺序执行：

```
1. 读取 `.cursor/rules/dev-rules.mdc`（本规则文件）
2. 确认本次变更属于哪个类型（对照"变更完成检查清单"）
3. 读取所有需要同步的文档，确认当前版本号
4. 执行代码变更
5. 变更完成后，同步更新所有相关文档的版本号和变更日志
```

> **注意**：规则存在于文件中并不意味着自动生效。每次新任务开始时，必须主动读取并遵循。
