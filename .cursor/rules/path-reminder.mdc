---
description: 工作目录路径提醒
alwaysApply: true
version: v1.0.0
lastUpdated: 2026-04-28
---

# 工作目录路径提醒

## 重要路径

**项目根目录**：`D:\Steam全域游戏搜索`

> 注意：是"**全域**"（quán yù），不是"全局"！

## 使用规范

1. **Shell 命令的 `working_directory`**：必须使用 `D:\Steam全域游戏搜索`，严禁使用 `D:\Steam全局游戏搜索`
2. **文件读取/写入**：使用 Glob/Read/Write 工具时，路径中必须包含 "全域"
3. **相对路径**：推荐使用相对路径（不指定 working_directory），避免手动输入完整路径

## 常见错误

| 错误写法 | 正确写法 |
|---------|---------|
| `D:\Steam全局游戏搜索` | `D:\Steam全域游戏搜索` |
| `D:/Steam全局游戏搜索` | `D:/Steam全域游戏搜索` |

## 验证方法

如果不确定当前路径是否正确，可以运行：

```powershell
Get-Location
```

输出显示乱码是正常的（PowerShell 中文编码问题），以项目名称中的"全域"为准。
