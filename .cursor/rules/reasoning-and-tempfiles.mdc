---
description: 强制 AI 使用中文进行推理过程，并要求临时文件统一放在 temp 目录下
alwaysApply: true
version: v1.0.0
lastUpdated: 2026-05-06
---

# AI 推理与临时文件规范

## 核心原则

推理过程必须使用中文，临时文件必须统一管理。

## 推理过程语言要求

- **全程使用中文**：AI 在分析问题、设计方案、推导结论、解释代码等所有推理过程中，必须使用中文进行思考和表述
- **禁止混杂**：推理文本中不得混入英文、日文等其他语言（代码标识符和实际代码除外）
- **清晰表达**：推理过程应当条理清晰，便于用户理解 AI 的思考路径
- **全程使用中文**：AI 在分析问题、设计方案、推导结论、解释代码等所有推理过程中，必须使用中文进行思考和表述

## 临时文件管理要求

- **统一存放位置**：所有在解决问题过程中产生的临时文件（如调试输出、批处理脚本、进度记录、中间数据等）必须放置到项目根目录下的 `temp` 目录中
- **禁止污染项目根目录**：不得在项目根目录或其他业务目录中创建临时文件
- **目录自动创建**：如果 `temp` 目录不存在，AI 应自动创建后再使用
- **定期清理提醒**：AI 可以在完成任务后提醒用户是否需要清理 `temp` 目录中的文件

## 违规处理

违反本规则视为行为错误，应立即停止并纠正。
