---
description: 规则文档索引和优先级说明
alwaysApply: true
version: v1.1.0
lastUpdated: 2026-04-28
---

# 规则文档索引

## 概述

本项目规则文档分为 **4个核心文件**，涵盖语言规范、安全要求、开发流程等方面。

---

## 规则优先级

当规则冲突时，按以下优先级执行：

| 优先级 | 规则文件 | 说明 |
|--------|----------|------|
| 1 | `git-safety.mdc` | Git 操作安全（最高优先级） |
| 2 | `dev-rules.mdc` | 语言、数据安全、临时脚本、开发规范 |
| 3 | `path-reminder.mdc` | 路径提醒 |
| 4 | `karpathy-guidelines.mdc` | 编程行为指南 |

---

## 规则文件清单

### 1. dev-rules.mdc（核心规范）

**内容**：语言要求、文档版本同步、数据保护、**临时脚本管理**、任务状态持久化、变更检查清单

**关键规则**：
- 语言一致性（最高优先级）：所有输出必须使用简体中文
- 数据安全：修改数据代码前必须先备份
- 临时脚本：统一放到 `scripts/temp/` 目录
- 任务状态持久化：使用 `.cursor/tasks/current-task.md` 追踪任务

### 2. git-safety.mdc（安全规范）

**内容**：Git 操作安全规范

**关键规则**：
- 没有明确指令，不得执行任何 git 相关操作
- 危险操作（checkout、reset、rebase）必须预警并确认

### 3. path-reminder.mdc（路径提醒）

**内容**：工作目录路径提醒

**关键规则**：
- 项目根目录：`D:\Steam全域游戏搜索`（注意是"全域"不是"全局"）

### 4. karpathy-guidelines.mdc（编程指南）

**内容**：编程行为指南（中英双语）

**关键规则**：
- 编码前先思考（不要假设，展示权衡）
- 简洁优先（用最少代码解决问题）
- 精准改动（只改必须改的）
- 目标驱动（定义成功标准）

---

## 冲突处理

如果两个规则产生冲突：
1. **语言规则与其他规则冲突**：语言规则优先
2. **安全规则与其他规则冲突**：安全规则优先
3. **其他规则冲突**：遵循 `dev-rules.mdc` 中的定义

---

## 更新日志

| 版本 | 日期 | 变更内容 |
|------|------|----------|
| v1.0.0 | 2026-04-28 | 初始版本，整合规则文档 |
| v1.1.0 | 2026-04-28 | 合并 temp-scripts.mdc 到 dev-rules.mdc，规则文件从5个精简为4个 |
